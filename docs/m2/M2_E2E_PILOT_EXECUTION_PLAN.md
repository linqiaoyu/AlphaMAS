# M2 E2E_PILOT Execution Plan

The E2E pilot runs exactly AAPL, AMZN, JPM, JBSS, EML, AGI, ARR, and AEMD at the 2023-10-06 close. It validates production integration only, after architecture `35c5a46616f654ce70b0badc01cd59fb0afd433dcdcd99e5e0b8f2419ec4d153` and ablations `e7e2cc8520f9033e57c442b04c882baffd7c51b497bbc9495b71098f64a83b0e` are committed and remote-verified.

Each symbol starts from an independent C09-derived state. The complete formal-like DeepSeek stack runs once: `deepseek-v4-flash`, thinking disabled, temperature zero, Medium/3/3 research and risk depth, all four analysts, English output, M0 historical-safe evidence plus the exact M2-06 frozen packet. Qwen3-VL calls remain zero. The first complete correctness-valid trajectory is official; performance cannot trigger a rerun or any architecture/configuration change.

Before the first paid call, the exact detached Phase-A SHAs, canonical existing A10G instance/EBS, cost guard, auto-stop, packet hashes, checkpoint, encoder, and market snapshot are verified. A conservative no-cache projection must remain below ¥10 DeepSeek and USD 3 incremental AWS. Credentials enter only the paid subprocess environment and never appear in logs or artifacts.

After all eight official trajectories are frozen, recorded LLM outputs feed a deterministic downstream audit-only replay. It makes no DeepSeek call and cannot replace an official trajectory. Because there is one decision per symbol, the terminal five-session credit is scored and archived without a post-horizon update.

Any missing/hash-invalid packet, lineage drift, protected access, cost breach, or correctness defect stops M2-14. FINAL_HOLDOUT and Formal 2024H1 remain entirely unexecuted and uninspected.
